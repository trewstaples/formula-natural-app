import { Box } from '@mui/system';

const styles = {
  container: {
    width: '1312px',
    margin: 'auto',
  },
};

const Container = () => {
  return <Box sx={styles.container} />;
};

export { Container };
