import { Box } from '@mui/system';

const styles = {
  container: {
    width: '1312px',
    marginTop: '1000px',
  },
};

const Container = () => {
  return <Box sx={styles.container} />;
};

export { Container };
